import { EnergyProductionData } from './energy-production-data';

describe('EnergyProductionData', () => {
  it('should create an instance', () => {
    expect(new EnergyProductionData()).toBeTruthy();
  });
});
